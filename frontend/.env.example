# The frontend needs no build-time secrets.
# Authentication is username/password, created on first run; the session
# token lives in localStorage and is sent as an Authorization: Bearer header
# by src/api/client.ts. There is nothing to configure here for local dev.
